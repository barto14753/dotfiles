set number
set relativenumber

" Set the timeout length to a shorter duration (e.g., 300 milliseconds)
set timeoutlen=300

" Map j+k to <Esc> in insert mode
inoremap jk <Esc>
syntax enable
colorscheme slate

set ts=4 sw=4

